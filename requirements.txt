# ---- 核心（最小可运行集合）----
langgraph>=0.2.0
langchain-core>=0.3.0
openai>=1.40.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
streamlit>=1.30.0

# ---- 可选增强（不装则自动降级）----
# 本地向量库（不装 -> 关键词检索）
chromadb>=0.5.0
# 长期记忆（不装 -> 本地 JSON 存储）
mem0ai>=0.1.0
# 多语言本地 embedding（EMBEDDING_BACKEND=sentence_transformers 时需要）
# sentence-transformers>=3.0.0

# ---- SQLite checkpointer（CHECKPOINTER=sqlite 时需要）----
# langgraph-checkpoint-sqlite>=2.0.0
